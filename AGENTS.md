# AGENTS.md

## 1. 项目目标

本项目是一个开源音乐播放软件，初期采用 `Vite + React + TypeScript` 技术栈，优先实现本地可运行的 MVP。

项目围绕许嵩已发行歌曲的元数据进行专辑分类展示，核心能力包括：

- 按专辑展示歌曲目录。
- 由开发者手动维护专辑和歌曲元数据。
- 用户选择自己合法拥有的本地音频文件进行播放。
- 用户创建临时歌单。
- 用户把单曲或整张专辑加入临时歌单。
- 用户拖拽调整临时歌单中的歌曲顺序。
- 用户设置临时歌单中每首歌本次播放次数，默认 1 次。

项目后期可能上线，因此实现时必须避免把数据源、音频路径、部署地址、用户配置写死在代码里。

## 2. 开发原则

- 每次只完成一个明确任务，避免一次性实现过多功能。
- 优先实现 MVP 所需的最小闭环，不提前构建复杂架构。
- 保持模块边界清晰：元数据、播放队列、播放器、本地文件映射、本地存储应分层处理。
- 优先使用项目既有模式和目录结构，不随意引入新的抽象。
- 新增依赖必须有明确理由，避免为了小功能引入大型库。
- UI 先服务核心使用流程：浏览专辑、加入队列、排序、设置播放次数、播放本地音频。
- 任何后期上线能力都应以可扩展接口预留，不应影响当前本地 MVP 的简洁性。

## 3. 版权与开源约束

- 仓库不得提交未经授权的音频文件，包括但不限于 `.mp3`、`.flac`、`.wav`、`.aac`、`.m4a`、`.ogg`。
- 仓库不得提交未经授权的歌词文件，包括但不限于 `.lrc`、`.srt`、`.ass` 或完整歌词文本。
- 仓库不得提交未经授权的专辑封面、艺人照片、MV 截图或商业平台图片。
- 仓库不得包含第三方音乐平台抓取的大段简介、评论、歌词、封面链接或受版权保护内容。
- 示例资源只能使用自制占位资源、空白资源或明确允许开源分发的测试资源。
- 项目文档和界面应明确说明：本项目不提供、不托管、不分发任何未经授权的音乐、歌词或封面资源。
- 用户本地音频文件只能由用户主动选择或授权访问，应用不应上传、不应复制进仓库、不应默认缓存到可分发目录。

## 4. 目录结构约定

推荐目录结构如下：

```text
src/
  app/                  应用入口、路由和页面骨架
  features/
    catalog/            专辑和歌曲浏览
    queue/              临时歌单、排序、播放次数
    player/             播放控制和音频播放状态
    local-library/      本地音频文件选择与映射
  domain/               核心类型和领域规则
  data/
    catalog/            手动维护的专辑和歌曲元数据
  infra/
    storage/            IndexedDB、Dexie、本地持久化
    audio/              HTMLAudioElement 封装
  components/           通用 UI 组件
  styles/               全局样式
docs/                   产品、版权、元数据维护文档
tests/                  自动化测试
public/
  placeholders/         自制占位资源，不放真实封面
```

开发时应尽量遵守以上边界。如果需要调整结构，必须在修改总结中说明原因。

## 5. 代码风格

- 使用 TypeScript，避免无必要的 `any`。
- 使用函数组件和 React Hooks。
- 组件命名使用 `PascalCase`。
- 变量、函数、文件夹命名优先使用清晰英文。
- 业务类型放在 `src/domain/` 或对应 feature 的 `types.ts` 中。
- 播放队列、播放次数、元数据校验等核心规则应写成可测试的纯函数。
- 避免在组件中堆积复杂业务逻辑。
- 避免硬编码本地绝对路径、远程接口地址、音频 URL、封面 URL。
- UI 文案使用中文，代码标识使用英文。
- 注释只用于解释不直观的业务规则或技术取舍，不写无意义注释。

## 6. 数据维护原则

- 专辑和歌曲元数据由开发者手动维护。
- 不要编造完整歌曲数据。
- 如果歌曲、专辑或曲序信息不确定，应使用少量占位样例，或在文档中提醒开发者后续手动核对。
- 元数据应使用稳定 ID，不依赖数组下标。
- 同一首歌的元数据对象应与临时歌单队列项分离。
- 播放次数属于临时歌单中的队列项，不属于歌曲元数据。
- 同一首歌可以在临时歌单中出现多次，每次应生成独立队列项。
- 本地音频绑定关系应保存在本地存储中，不应写入开发者维护的歌曲元数据文件。
- 后期如接入远程元数据源，应保留本地元数据和远程元数据的迁移或覆盖策略。

## 7. 测试要求

新增或修改核心逻辑时，应优先补充自动化测试。

至少应覆盖以下规则：

- 单曲加入临时歌单。
- 整张专辑按曲序加入临时歌单。
- 临时歌单拖拽排序后的顺序更新。
- 队列项播放次数默认值为 1。
- 播放次数只能为正整数。
- 同一首歌在队列中出现多次时，各队列项播放次数互不影响。
- 未绑定本地音频时，播放器不会尝试播放不存在的资源。
- 元数据 ID 稳定且引用关系有效。

如果因为项目阶段或环境限制无法运行测试，修改总结中必须说明未运行原因和残余风险。

## 8. 每次修改后的总结要求

每次完成修改后，回复中必须包含：

- 改了什么：列出关键文件和核心变更。
- 如何验证：说明运行了哪些命令、测试或手动检查。
- 还有什么风险：说明未覆盖的边界、未验证的环境或后续注意事项。

总结应简洁具体，不写空泛描述。

## 9. 禁止事项

- 禁止提交未经授权的音频、歌词、专辑封面、艺人照片、MV 截图。
- 禁止把商业音乐平台的抓取内容直接写入仓库。
- 禁止编造完整许嵩歌曲数据来冒充已核对元数据。
- 禁止把用户本地音频路径硬编码到代码或元数据中。
- 禁止把未来线上 API 地址、音源地址、部署地址写死。
- 禁止一次性实现多个无关功能。
- 禁止在没有明确需求时引入账号、云同步、推荐、评论、排行榜等非 MVP 功能。
- 禁止为了方便测试而放入真实受版权保护资源。
- 禁止在未说明理由的情况下大规模重构目录或架构。
- 禁止删除或覆盖用户已有改动，除非用户明确要求。
