# 2026-07-18 开发交接

> 本文记录 2026-07-18（Asia/Shanghai）确认的目录导入与可选 QQ 元数据方案。它只约束后续实现，不包含音频、歌词、封面、Cookie 或平台抓取数据。

## 当前状态

- 当前分支：`task/next-01-safe-catalog-deletion`。
- NEXT-01 已提交：`021bb18 feat(catalog): make deletions irreversible`。删除目录记录会级联移除临时歌单项和 IndexedDB 音频绑定；不会删除用户磁盘原文件，也没有恢复入口。
- NEXT-01 完成时已通过 lint、build、193 项测试、相关 Prettier 检查和 `git diff --check`。
- NEXT-02 已提交：`9676eef feat(local-library): scan selected music directories`。新增纯目录扫描器与 6 项单元测试；扫描器只按当前支持的扩展名产生候选，保留目录路径、文件名、歌手/歌名草稿和机器可读的需确认原因；不访问目录、歌单、音频仓储或网络。
- NEXT-03 已完成待提交：新增纯曲目匹配器与 4 项匹配场景测试。它只读取当前有效目录和本地绑定映射，以规范化歌名严格匹配；同名、重复文件、已绑定和扫描异常不会产生可自动绑定结果。
- `docs/improve.md` 是用户自有未跟踪调研记录，始终不修改、不暂存、不提交。

## 已确认的导入规则

- 用户选择单张专辑目录或音乐库根目录均可；每个文件的直接父目录是候选专辑名，不要求固定“音乐库”目录。
- 文件可为 `歌曲名.mp3` 或 `歌手名-歌曲名.flac`；歌手可省略。预览页提供可编辑的本次默认歌手，当前可预填“许嵩”，但扫描与匹配纯函数不得硬编码它。
- `.ext` 是任意扩展名占位，而非实际格式限制；扫描候选支持 `.mp3`、`.flac`、`.m4a`、`.aac`、`.ogg`、`.opus`、`.wav`。能否播放取决于浏览器的实际编码支持。
- 以规范化歌名精确匹配为主。只有唯一内部 `trackId` 才可建议绑定；重名、版本差异、歌手不一致、未知前缀和已有绑定都必须人工确认。
- 文件顺序不决定播放顺序。临时歌单控制播放；目录曲序只是可选展示信息。
- 扫描不写入。预览页可以把未匹配目录生成“专辑名 + 歌曲名 + 默认/解析歌手”的本地草稿，但用户必须显式确认才创建专辑、歌曲与绑定。

## QQ 元数据边界

- 固定流程：用户配置服务地址 → 手动测试或查询 → 预览差异 → 确认导入 → 本地默认展示。不得启动时实时请求或硬编码 `localhost:3200`。
- 本地 `/explorer/metadata` 可访问，但 2026-07-18 搜索“许嵩”时上游在服务端 10 秒超时；Provider 必须处理超时和不可用，且不能阻断本地目录。
- 只保存必要事实字段和可选外部引用；内部稳定 ID 不使用外部 MID。绝不保存或播放音源 URL，也不保存 Cookie、歌词、封面、评论或原始响应。

## 下一步

下一项为 `NEXT-04`（导入预览与批量绑定）：只允许用户确认后写入；默认仅勾选一对一、未绑定的精确命中。冲突、候选、已绑定、未匹配和扫描异常都必须保留人工处理入口。之后再实施 `SOURCE-01`、`SOURCE-02`；Provider 可以复用草稿预览，但绝不是目录导入的前置条件。

开始前检查：

```powershell
git status --short --branch
git log --oneline --decorate -5
git diff --check
```

## NEXT-04 完成记录

- 已完成目录导入预览与受控批量绑定：选择目录后只扫描和匹配，不会立即写入；取消预览也不会写入任何目录或音频数据。
- 默认仅勾选唯一、未绑定的精确匹配；候选、冲突、已绑定、未匹配和扫描异常均保留人工处理。冲突必须明确选择目标歌曲，批量导入不会覆盖已有绑定或接受重复目标。
- 未匹配的有效文件可按候选专辑生成草稿，必须明确勾选。默认歌手只能唯一匹配现有目录艺人；同歌手同名专辑不会自动重复创建。新专辑曲序按稳定相对路径排序，仅用于目录展示，不影响临时歌单播放顺序。
- 新专辑元数据以一次 LocalStorage 保存提交；成功后才逐项保存 IndexedDB 音频绑定。两类存储不伪装为原子事务：目录保存失败时不会写该组音频；音频部分失败会保留已成功项并显示失败文件。
- 新增 11 项测试（导入计划、导入集成、批量重复目标与并发保护）；当前完整检查为 15 个测试文件、214 项测试通过，并已通过 lint、build、Prettier 与 `git diff --check`。

下一项：按既有边界继续 `SOURCE-01`，只实现可配置、手动触发的元数据 Provider 接口与预览，不把本地目录导入依赖在 Provider 上。

## SOURCE-01 完成记录

- 已定义平台无关的元数据 Provider 描述、能力、搜索、专辑详情和歌曲详情契约。候选只使用规范化的艺人、专辑、歌曲字段与带实体类型的外部引用；Provider ID 和外部 ID 不是本地目录 ID。
- 已新增“专辑候选转本地草稿”和只读差异预览纯函数。它们不会保存目录、不会改写已有内部 ID，也只把名称相同的现有记录标为“可能已存在”，仍需后续 UI 由用户确认。
- Provider 契约和导入草稿均明确不含音频 URL、Cookie、歌词、封面、评论、长简介或原始响应；没有 Provider 实例被应用启动流程引用，因此未配置、不可用或超时时本地目录不会受影响。
- 新增 fake Provider、草稿规范化、差异只读和畸形/重复引用测试；当前完整检查为 16 个测试文件、218 项测试通过，并已通过 lint、build、Prettier 与 `git diff --check`。

下一项：实施 `SOURCE-02` 前先复核自托管 API 的响应契约以及许可和使用条款；适配器必须保持默认关闭、由用户配置地址并手动触发，且不能把 `localhost:3200`、Cookie、音频地址或抓取响应写入仓库。

## 许嵩正式目录导入完成记录

- 已加入需预览并确认的许嵩正式专辑类目录导入：经逐张核对后包含 11 张专辑、111 首歌曲；只新增当前目录不存在的同名专辑，不覆盖现有元数据，不删除占位目录，也不写入或绑定音频。
- 元数据只保留艺人名、专辑名、歌曲名和曲序。曲目事实在 2026-07-18 基于 MusicBrainz 核心数据逐张核对；范围、许可边界及后续维护规则见 `docs/metadata.md`。单曲、影视原声、精选合辑和重复发行版本仍未纳入，必须另行核对。
- 本地 QQ 服务的 `/explorer/metadata` 可访问，但搜索“许嵩”会在其自身上游 10 秒超时。`SOURCE-02` 仍待实施：必须默认关闭、由用户配置地址并手动触发，且丢弃原始响应与任何 Cookie、音源、歌词、封面或评论字段。

## 专辑操作菜单完成记录

- 专辑详情保留“整张加入”作为可见主操作；“编辑专辑”“添加歌曲”和“删除专辑”统一收进三点更多操作菜单，减少页面按钮数量。
- 菜单以 Portal 渲染，支持点外关闭、Esc 关闭并将焦点还给触发按钮，以及在滚动或窗口缩放时关闭。删除入口仍使用原有的二次确认与级联清理流程，不会删除用户磁盘中的音频文件。
- 已补充菜单收起、展开、可访问标签、Esc 关闭及焦点回归测试；完整套件为 18 个测试文件、223 项通过。全仓格式检查仅受用户未跟踪的 `docs/improve.md` 影响，该文件保持未修改、未暂存。

## PLAYER-01 完成记录

- 本地播放器现在监听 `loadedmetadata`、`durationchange`、`timeupdate`、`seeking` 与 `seeked`，并仅在当前音源 token 有效时更新媒体时间。切歌、重新绑定、释放音源、无效时长或播放错误都会清空进度，避免展示旧音源时长。
- 播放栏顶部新增横贯全宽的进度轨道，左右显示当前/总时长；未加载有效本地时长时显示 `--:--` 并禁用定位。轨道可用键盘或拖动定位，但不会改变临时歌单 occurrence；圆点只在悬停、聚焦或拖动时显示。
- 已新增媒体事件集成测试，覆盖加载、`timeupdate`、定位、无效时长和切歌重置。完整自动化检查为 18 个测试文件、224 项通过；桌面与窄屏布局均用临时本地无头浏览器截图检查，截图和浏览器配置均已删除，未纳入仓库。

## PLAYER-02 完成记录

- 新增独立、版本化的播放器设置仓储，只保存音量（0 到 1）和静音状态；不保存播放、进度、队列、音频 URL 或媒体事件 token。损坏 JSON、未知 schema、越界音量和错误静音值均回退到安全默认值，不会传播给 `HTMLAudioElement`。
- 音量和静音会即时同步到当前及后续创建的本地音频元素；两者相互独立，调整音量不会解除静音。刷新只恢复设置，播放器仍从安全暂停/空队列状态启动。
- 播放栏增加紧凑静音按钮、音量滑块与百分比；桌面布局已用临时本地无头浏览器截图检查，截图和浏览器配置已删除。已补仓储损坏数据、边界、即时同步和刷新恢复测试。

## NEXT-05 完成记录

- 目录导入保留原有“选择音乐目录（复制文件）”兼容流程；支持 File System Access API 的浏览器会额外显示明确的“选择目录（不复制，实验性）”操作。后者递归读取用户选择的目录，只在本次预览中保留相对路径，确认后将每首文件句柄（而非 `File` 副本）写入 IndexedDB。
- 本地音频记录现按 `file-copy` 和 `file-handle` 两种方式区分。旧 IndexedDB 中没有方式字段的有效 `File` 记录会在读取时安全迁移为 `file-copy`，不会丢失既有绑定；两种方式均不保存绝对路径。
- 句柄记录在启动恢复时会以只读方式检查权限。`prompt`、`denied` 和检查异常均标记为“需要重新授权”，播放器不会把它当作可播放文件；用户可在歌曲行点击“重新授权”。取得授权后，播放层才通过句柄读取原文件并生成临时 `blob:` URL。
- 目录导入区域会列出浏览器副本和原文件引用的数量及大小，并解释当前导入的存储方式。原文件引用的大小仅作曲库规模提示，不占用浏览器音频副本空间。
- 已新增能力探测、目录递归读取、不可读文件跳过、权限状态转换和句柄记录读取测试。完整自动化检查为 20 个测试文件、236 项通过；构建通过。真实 Chrome/Edge 刷新后的句柄结构化克隆和授权恢复仍需在目标部署来源人工验证，File System Access API 继续是可选增强而非唯一模式。

## UI-01 完成记录

- 页面顶部新增“跳到主要内容”键盘入口，主工作区有可聚焦目标；按钮、表单、链接现在共享可见焦点样式，并为强制颜色模式保留系统焦点颜色。减少动态效果偏好会关闭非必要的过渡和动画。
- 已收紧窄屏的导入、目录和播放器布局。目录预览表的 690px 最小宽度只会在自身容器内横向滚动，不再撑宽整个工作区；在 390px 浏览器视口实测 `scrollWidth` 为 375px、工作区为 351px，没有页面横向溢出。
- 目录、歌单、播放器继续保留原本的按钮式排序、重复次数、导入和播放控制路径。播放器现在会把句柄绑定的 `permission_required` 或 `missing` 状态直接说明为“需要重新授权”或“原文件不可用”，不会将其表述成正常已绑定。
- 新增键盘入口和授权状态的组件测试；完整自动化检查、构建、lint、变更文件 Prettier 检查与 `git diff --check` 均须在提交前复跑。临时无头浏览器截图、调试配置和检查脚本已删除，未纳入仓库。

## UI-02 导入工具栏与弹窗完成记录

- 目录页不再在专辑列表上方长期展开“导入已核对的许嵩目录”和“导入本地音乐目录”两张大卡片；它们收纳为目录标题右侧的“导入工具”菜单栏，分别以“导入许嵩目录”和“批量绑定音频”打开独立弹窗。
- 两种导入流程的扫描、预览、确认写入和本地音频绑定逻辑均未改变。关闭弹窗不会丢失本次预览状态；打开时支持关闭按钮、背景点击和 Esc，关闭后焦点回到对应工具按钮。关闭状态不再暴露为活跃 `dialog`，因此不会干扰既有弹窗交互。
- 已补充批量绑定入口的打开、关闭、焦点回归和隐藏样式测试。桌面和 390px 窄屏均以本地无头浏览器实测：移动端标题区改为纵向顺序，工具栏可完整显示且不产生横向溢出。临时截图与浏览器配置已删除，不纳入仓库。
- 本次验证：21 个测试文件、240 项测试通过；`npm.cmd run lint` 与 `npm.cmd run build` 通过。提交前仍应执行变更文件 Prettier 检查和 `git diff --check`。

## UI-03 歌曲更多操作菜单完成记录

- 专辑歌曲行现在只保留音频绑定状态、三个点更多操作与“加入临时歌单”主操作，避免编辑、删除、绑定/更换、重新授权和解绑等次要操作挤占每一行。
- 三个点菜单沿用专辑更多操作的交互边界：点击外部、滚动、窗口缩放或 Esc 都会关闭；Esc 会将焦点交还给对应歌曲的触发按钮。原有单曲元数据编辑、音频文件选择、句柄重新授权、解绑和目录删除均保留在菜单中，动作语义与业务流程未变。
- 已补充目录歌曲菜单的隐藏、展开、可访问标签、文件选择入口与 Esc 焦点回归测试，并将既有的单曲编辑与音频绑定/解绑测试改为通过菜单触发。桌面和 390px 窄屏已用本地无头浏览器检查，临时截图与浏览器配置已删除，未纳入仓库。
- 本次验证：21 个测试文件、241 项测试通过；`npm.cmd run lint`、`npm.cmd run build` 通过。提交前仍应执行变更文件 Prettier 检查和 `git diff --check`。

## UI-04 播放器安全区与等高歌单完成记录

- 桌面端页面为固定播放器预留了 160px 安全区，再叠加播放器的 24px 底部距离；长专辑的最后一首现在可以完整滚动到播放器上方，不会被固定栏遮住。
- 桌面端临时歌单取消固定的视口高度，改为在应用双栏网格中拉伸到与专辑工作区同高；歌单项目仍在自己的列表中滚动。小于等于 860px 时仍沿用原有单列布局、固定歌单高度与非固定播放器，不受这项桌面改动影响。
- 已以本地无头浏览器检查桌面左右面板底边对齐和 390px 窄屏布局；临时截图与浏览器配置已删除，未纳入仓库。本次验证：21 个测试文件、241 项测试通过；`npm.cmd run lint`、`npm.cmd run build` 通过。

## 已保存歌单与独立播放快照：后续设计已确认

- 下一条功能链的完整实现约束见 [saved-playlist-library.md](../tasks/saved-playlist-library.md)。它将“当前编辑歌单”“已保存歌单库”和“活动播放快照”明确分层：切换或编辑其他歌单绝不隐式改变正在播放的 occurrence 序列。
- UI 采用歌单面板内部的窄收藏方块栏：顶部固定 `＋` 方块，其他方块代表已保存歌单；桌面端置于歌单内部右侧，窄屏改为标题下方横向滚动。方块选中态与播放中来源标记必须能同时表达且可区分。
- 后续实现必须按 `PLAYLIST-01`（纯规则与存储）→ `PLAYLIST-02`（播放快照解耦）→ `PLAYLIST-03`（方块栏 UI）→ `PLAYER-03`（歌曲行快速播放）逐项推进，不能在 UI 层继续沿用“编辑歌单即同步播放器”的现有行为。
- 专辑歌曲快速播放固定为单曲一次且不修改歌单；歌单内歌曲快速播放固定从该队列项第一次 occurrence 开始。首版不实现双击播放。首次保存歌单现已确定为名称必填：不得静默生成名称或覆盖来源歌单。

## PLAYLIST-01 完成记录

- 已新增歌单库领域类型、创建副本、重命名、删除和选择校验的纯函数。保存副本会重置运行时 `playedCount`，但保留队列项顺序、重复次数和来源信息；不同歌单中的同一首歌或同名队列项互不共享可变状态。
- 已新增独立、版本化的 `vae-music:playlist-library:v1` 仓储。它只序列化歌单文档和稳定队列数据，拒绝运行时播放状态、媒体 token、`File`、`blob:` URL 和音频文件名。旧 `vae-music:temporary-playlist:v1` 仅在新 key 不存在时以只读方式迁移为临时草稿，不会被覆盖或清除。
- 本任务未接入 `App`、播放器或歌单 UI；下一项应严格实施 `PLAYLIST-02` 的播放快照与编辑状态解耦，并同时更新目录删除联动。
