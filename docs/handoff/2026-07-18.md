# 2026-07-18 开发交接

> 本文记录 2026-07-18（Asia/Shanghai）确认的目录导入与可选 QQ 元数据方案。它只约束后续实现，不包含音频、歌词、封面、Cookie 或平台抓取数据。

## 当前状态

- 当前分支：`task/next-01-safe-catalog-deletion`。
- NEXT-01 已提交：`021bb18 feat(catalog): make deletions irreversible`。删除目录记录会级联移除临时歌单项和 IndexedDB 音频绑定；不会删除用户磁盘原文件，也没有恢复入口。
- NEXT-01 完成时已通过 lint、build、193 项测试、相关 Prettier 检查和 `git diff --check`。
- NEXT-02 已提交：`9676eef feat(local-library): scan selected music directories`。新增纯目录扫描器与 6 项单元测试；扫描器只按当前支持的扩展名产生候选，保留目录路径、文件名、歌手/歌名草稿和机器可读的需确认原因；不访问目录、歌单、音频仓储或网络。
- NEXT-03 已完成待提交：新增纯曲目匹配器与 4 项匹配场景测试。它只读取当前有效目录和本地绑定映射，以规范化歌名严格匹配；同名、重复文件、已绑定和扫描异常不会产生可自动绑定结果。
- `docs/improve.md` 是用户自有未跟踪调研记录，始终不修改、不暂存、不提交。

## 已确认的导入规则

- 用户选择单张专辑目录或音乐库根目录均可；每个文件的直接父目录是候选专辑名，不要求固定“音乐库”目录。
- 文件可为 `歌曲名.mp3` 或 `歌手名-歌曲名.flac`；歌手可省略。预览页提供可编辑的本次默认歌手，当前可预填“许嵩”，但扫描与匹配纯函数不得硬编码它。
- `.ext` 是任意扩展名占位，而非实际格式限制；扫描候选支持 `.mp3`、`.flac`、`.m4a`、`.aac`、`.ogg`、`.opus`、`.wav`。能否播放取决于浏览器的实际编码支持。
- 以规范化歌名精确匹配为主。只有唯一内部 `trackId` 才可建议绑定；重名、版本差异、歌手不一致、未知前缀和已有绑定都必须人工确认。
- 文件顺序不决定播放顺序。临时歌单控制播放；目录曲序只是可选展示信息。
- 扫描不写入。预览页可以把未匹配目录生成“专辑名 + 歌曲名 + 默认/解析歌手”的本地草稿，但用户必须显式确认才创建专辑、歌曲与绑定。

## QQ 元数据边界

- 固定流程：用户配置服务地址 → 手动测试或查询 → 预览差异 → 确认导入 → 本地默认展示。不得启动时实时请求或硬编码 `localhost:3200`。
- 本地 `/explorer/metadata` 可访问，但 2026-07-18 搜索“许嵩”时上游在服务端 10 秒超时；Provider 必须处理超时和不可用，且不能阻断本地目录。
- 只保存必要事实字段和可选外部引用；内部稳定 ID 不使用外部 MID。绝不保存或播放音源 URL，也不保存 Cookie、歌词、封面、评论或原始响应。

## 下一步

下一项为 `NEXT-04`（导入预览与批量绑定）：只允许用户确认后写入；默认仅勾选一对一、未绑定的精确命中。冲突、候选、已绑定、未匹配和扫描异常都必须保留人工处理入口。之后再实施 `SOURCE-01`、`SOURCE-02`；Provider 可以复用草稿预览，但绝不是目录导入的前置条件。

开始前检查：

```powershell
git status --short --branch
git log --oneline --decorate -5
git diff --check
```

## NEXT-04 完成记录

- 已完成目录导入预览与受控批量绑定：选择目录后只扫描和匹配，不会立即写入；取消预览也不会写入任何目录或音频数据。
- 默认仅勾选唯一、未绑定的精确匹配；候选、冲突、已绑定、未匹配和扫描异常均保留人工处理。冲突必须明确选择目标歌曲，批量导入不会覆盖已有绑定或接受重复目标。
- 未匹配的有效文件可按候选专辑生成草稿，必须明确勾选。默认歌手只能唯一匹配现有目录艺人；同歌手同名专辑不会自动重复创建。新专辑曲序按稳定相对路径排序，仅用于目录展示，不影响临时歌单播放顺序。
- 新专辑元数据以一次 LocalStorage 保存提交；成功后才逐项保存 IndexedDB 音频绑定。两类存储不伪装为原子事务：目录保存失败时不会写该组音频；音频部分失败会保留已成功项并显示失败文件。
- 新增 11 项测试（导入计划、导入集成、批量重复目标与并发保护）；当前完整检查为 15 个测试文件、214 项测试通过，并已通过 lint、build、Prettier 与 `git diff --check`。

下一项：按既有边界继续 `SOURCE-01`，只实现可配置、手动触发的元数据 Provider 接口与预览，不把本地目录导入依赖在 Provider 上。

## SOURCE-01 完成记录

- 已定义平台无关的元数据 Provider 描述、能力、搜索、专辑详情和歌曲详情契约。候选只使用规范化的艺人、专辑、歌曲字段与带实体类型的外部引用；Provider ID 和外部 ID 不是本地目录 ID。
- 已新增“专辑候选转本地草稿”和只读差异预览纯函数。它们不会保存目录、不会改写已有内部 ID，也只把名称相同的现有记录标为“可能已存在”，仍需后续 UI 由用户确认。
- Provider 契约和导入草稿均明确不含音频 URL、Cookie、歌词、封面、评论、长简介或原始响应；没有 Provider 实例被应用启动流程引用，因此未配置、不可用或超时时本地目录不会受影响。
- 新增 fake Provider、草稿规范化、差异只读和畸形/重复引用测试；当前完整检查为 16 个测试文件、218 项测试通过，并已通过 lint、build、Prettier 与 `git diff --check`。

下一项：实施 `SOURCE-02` 前先复核自托管 API 的响应契约以及许可和使用条款；适配器必须保持默认关闭、由用户配置地址并手动触发，且不能把 `localhost:3200`、Cookie、音频地址或抓取响应写入仓库。

## 许嵩正式目录导入完成记录

- 已加入需预览并确认的许嵩正式专辑类目录导入：经逐张核对后包含 11 张专辑、111 首歌曲；只新增当前目录不存在的同名专辑，不覆盖现有元数据，不删除占位目录，也不写入或绑定音频。
- 元数据只保留艺人名、专辑名、歌曲名和曲序。曲目事实在 2026-07-18 基于 MusicBrainz 核心数据逐张核对；范围、许可边界及后续维护规则见 `docs/metadata.md`。单曲、影视原声、精选合辑和重复发行版本仍未纳入，必须另行核对。
- 本地 QQ 服务的 `/explorer/metadata` 可访问，但搜索“许嵩”会在其自身上游 10 秒超时。`SOURCE-02` 仍待实施：必须默认关闭、由用户配置地址并手动触发，且丢弃原始响应与任何 Cookie、音源、歌词、封面或评论字段。

## 专辑操作菜单完成记录

- 专辑详情保留“整张加入”作为可见主操作；“编辑专辑”“添加歌曲”和“删除专辑”统一收进三点更多操作菜单，减少页面按钮数量。
- 菜单以 Portal 渲染，支持点外关闭、Esc 关闭并将焦点还给触发按钮，以及在滚动或窗口缩放时关闭。删除入口仍使用原有的二次确认与级联清理流程，不会删除用户磁盘中的音频文件。
- 已补充菜单收起、展开、可访问标签、Esc 关闭及焦点回归测试；完整套件为 18 个测试文件、223 项通过。全仓格式检查仅受用户未跟踪的 `docs/improve.md` 影响，该文件保持未修改、未暂存。
